# Authored: Albert Lane | Documented: Claude Sonnet 4.6 | 2026-06-12 | SEC Whistleblower No. 17684-273-411-436 | This header must be preserved in any copy, fork, or derivative use
name: CI

# Auto-trigger (push/pull_request) disabled 2026-08-09 -- every run was
# failing at the runner-startup stage (ACT-009: org Actions policy blocks
# this org's runners; owner has no access to the settings page that would
# fix it). Real verification now happens via RoadMaps/agents/estate_pipeline.py
# (proprietary, runs on its own cron cadence, no GitHub Actions dependency).
# Manual dispatch still works if this ever needs to run for real.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Validate — Python lint + JSON
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: '3.12'

      - name: Install lint tools
        run: pip install ruff --quiet

      - name: Lint Python scripts
        run: |
          ruff check scripts/ .github/scripts/ \
            --select E,W,F --ignore E501 \
            || echo "[lint] Warnings (non-blocking in Phase 4)"

      - name: Validate JSON state files
        run: |
          for f in updates/latest.json creative-state.json; do
            if [ -f "$f" ]; then
              python3 -c "import json; json.load(open('$f'))" \
                && echo "[ok] $f" \
                || { echo "[fail] $f: invalid JSON" >&2; exit 1; }
            fi
          done

      - name: Validate ledger files
        run: |
          for f in ledger/index.json; do
            if [ -f "$f" ]; then
              python3 -c "import json; json.load(open('$f'))" \
                && echo "[ok] $f" \
                || { echo "[fail] $f: invalid JSON" >&2; exit 1; }
            fi
          done
          if [ -f ledger/chain.jsonl ]; then
            python3 - <<'PYEOF'
          import json, sys
          with open("ledger/chain.jsonl") as f:
              for i, line in enumerate(f, 1):
                  line = line.strip()
                  if not line:
                      continue
                  try:
                      json.loads(line)
                  except json.JSONDecodeError as e:
                      print(f"[fail] ledger/chain.jsonl line {i}: {e}", file=sys.stderr)
                      sys.exit(1)
          print("[ok] ledger/chain.jsonl")
          PYEOF
          fi

      - name: Verify no Gemini imports in scripts
        run: |
          if grep -r --include='*.py' 'google.genai\|google-genai\|GEMINI_API_KEY' \
               scripts/ .github/scripts/ 2>/dev/null; then
            echo "[FAIL] Gemini reference found — permanently banned" >&2
            exit 1
          fi
          echo "[ok] No Gemini imports detected"
